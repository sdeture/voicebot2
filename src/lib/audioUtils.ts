/**
 * Re-export audio utilities for backward compatibility
 */

/**
import * as audioUtils from "../utils/audio.utils";

export const playBeep = audioUtils.playBeep;
export const playStartBeep = audioUtils.playStartBeep;

export default audioUtils;
 */
