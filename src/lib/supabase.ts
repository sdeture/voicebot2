/**
 * Re-export Supabase client for backward compatibility
 */

/**
import * as supabaseService from "../services/database/supabase.service";

export const supabase = supabaseService.supabaseClient;

export default supabase;
*/
